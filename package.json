{
  "name": "webex-message-action",
  "version": "1.0.0",
  "description": "GitHub Action to send messages to Cisco Webex",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "action",
    "webex",
    "cisco"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
} 